# 手机直接拿到 APK（不需要 Android Studio）

这个项目已经加入 GitHub Actions 自动编译。

## 操作
1. 在 GitHub 新建一个仓库，例如 `LysnAITranslator`。
2. 把这个 ZIP 解压后的所有文件上传到仓库。
3. 打开仓库顶部的 `Actions`。
4. 选择 `Build APK`。
5. 点 `Run workflow`。
6. 等待构建完成。
7. 打开这次运行记录，在页面底部的 `Artifacts` 下载 `LysnAITranslator-debug`。
8. 解压后得到 `app-debug.apk`，传到手机安装。

注意：
- 这是 debug APK，仅供你自己测试。
- DeepSeek API Key 不要写进源码或上传到 GitHub；安装后在 App 内填写。
